# «Мост» / сайт — конвенции для ИИ-агента

Этот файл включается в контекст КАЖДОГО промпта для Claude Code по проекту сайта (положить в корень репозитория как `CLAUDE.md` или подключать явно). Правила ниже — не пожелания, а инварианты проекта. Агент не имеет права их менять, «улучшать» или обходить без явного указания в промпте.

---

## 1. Язык и стек

- Язык проекта — **JavaScript**. Файлы: `.astro`, `.jsx`, `.js`, `.mjs`. Создавать `.ts` / `.tsx` файлы **запрещено**.
- `tsconfig.json` существует только ради алиасов путей (`@/*` → `src/*`) и подсказок редактора: `"extends": "astro/tsconfigs/base"`. Строгие режимы не включать.
- JSDoc-аннотации для нетривиальных функций — приветствуются (это наш инструмент ревью), но без фанатизма.
- Стек зафиксирован: Astro 5, React-острова, Tailwind CSS 4, shadcn/ui, MDX content collections. Новые зависимости — только по явному указанию в промпте; «удобную библиотеку» самовольно не добавлять.

## 2. UI-компоненты: shadcn/ui

- `components.json`: `"tsx": false` — компоненты генерируются в JSX. Проверять после каждого `npx shadcn add`, что в репо не появились `.tsx`.
- Компоненты shadcn живут в `src/components/ui/` и считаются нашим кодом: кастомизировать можно, но только через токены (см. §3–4), не хардкодом значений внутри компонента.
- Другие UI-библиотеки (MUI, Ant, Chakra, DaisyUI и т.п.) не использовать. Нет компонента в shadcn — собираем свой на Radix-примитивах + Tailwind.
- Иконки — lucide-react (идёт в паре с shadcn), другие иконочные паки не подключать.

## 3. Цвета: только RGB

- Единственный допустимый формат цвета — **`rgb(R, G, B)`** (при прозрачности — `rgb(R G B / A)`).
- **Запрещены**: hex, `hsl()`, `oklch()`, именованные CSS-цвета, произвольные hex в Tailwind (`bg-[#...]`).
- Именование — семантическое (по назначению материала: kobalt, malahit, surik...), не по абстрактным alias'ам (--color-1). Технические CSS-переменные в tokens.css (--bg, --accent и т.п.) остаются как есть — это их программные имена; таблица ниже — источник значений и роль каждого цвета.

### 3.1. Светлая тема

| Токен | RGB | Роль |
|---|---|---|
| `color-grafit` | rgb(28, 33, 43) | основной текст, тёмные секции |
| `color-grafit-surface` | rgb(37, 43, 56) | карточки на тёмном фоне (внутри светлой темы) |
| `color-grafit-line` | rgb(56, 63, 76) | бордеры на тёмном фоне |
| `color-stal` | rgb(93, 104, 122) | вторичный текст |
| `color-stal-light` | rgb(160, 170, 185) | пунктиры, второстепенные линии |
| `color-text-inverse` | rgb(178, 187, 200) | вторичный текст на тёмном фоне |
| `color-text-inverse-muted` | rgb(122, 132, 150) | подписи на тёмном фоне |
| `color-liniya` | rgb(219, 224, 231) | бордеры, разделители, мм-сетка |
| `color-bumaga` | rgb(247, 248, 250) | фон страницы |
| `color-tuman` | rgb(240, 243, 247) | hover-фоны, активный пункт меню |
| `color-belyi` | rgb(255, 255, 255) | поверхности карточек и форм |
| `color-kobalt` | rgb(38, 84, 210) | акцент CTA, ссылки, IT-сегмент, фокус |
| `color-kobalt-hover` | rgb(28, 62, 160) | hover кобальта |
| `color-kobalt-tint` | rgb(232, 239, 252) | подложка бейджей IT |
| `color-kobalt-light` | rgb(126, 158, 240) | акцент на тёмном фоне (внутри светлой темы) |
| `color-malahit` | rgb(15, 122, 101) | бизнес-сегмент, успех форм |
| `color-malahit-hover` | rgb(11, 95, 79) | hover малахита |
| `color-malahit-tint` | rgb(230, 243, 239) | подложка бейджей бизнеса, успех-фон |
| `color-surik` | rgb(198, 60, 51) | ошибки форм |
| `color-surik-tint` | rgb(251, 238, 237) | подложка ошибок |
| `color-surik-border` | rgb(233, 190, 186) | бордер блока ошибки |

### 3.2. Тёмная тема (`html[data-theme="dark"]`)

| Токен | RGB | Роль |
|---|---|---|
| `color-dark-bg` | rgb(17, 21, 28) | фон страницы |
| `color-dark-surface` | rgb(26, 31, 41) | поверхности карточек и форм |
| `color-dark-ink` | rgb(235, 239, 245) | основной текст |
| `color-dark-muted` | rgb(148, 159, 176) | вторичный текст |
| `color-dark-line` | rgb(54, 61, 74) | бордеры, разделители, мм-сетка |
| `color-dark-hover` | rgb(38, 45, 58) | hover-фоны |
| `color-dark-kobalt` | rgb(99, 134, 235) | акцент CTA, ссылки, фокус |
| `color-dark-kobalt-hover` | rgb(146, 172, 246) | hover акцента |
| `color-dark-kobalt-tint` | rgb(32, 43, 68) | подложка бейджей IT |
| `color-dark-malahit` | rgb(46, 166, 139) | бизнес-сегмент, успех форм |
| `color-dark-malahit-tint` | rgb(23, 48, 42) | подложка бейджей бизнеса |
| `color-dark-surik` | rgb(233, 108, 98) | ошибки форм |
| `color-dark-section` | rgb(23, 28, 37) | тёмные секции и футер |

Правила темизации без изменений (см. §3.3 старой версии): переключение только через `data-theme`, компоненты не знают о теме, только токены.

## 4. Размеры: только px

- Все размерные значения — **в пикселях**. Запрещены `rem`, `em`, `ch`, `vw/vh` (кроме layout-задач вроде `width: 100%`).

### 4.1. Типографика (desktop / mobile 390)

| Роль | Шрифт/вес | Размер/межстрочный | Letter-spacing | Моб. размер/межстрочный | Назначение |
|---|---|---|---|---|---|
| `font-display` | Onest 700 | 64px/70px | −0.5px | 38px/42px | hero главной |
| `font-h1` | Onest 700 | 56px/62px | −0.5px | 36px/40px | заголовки страниц |
| `font-h2` | Onest 700 | 40px/46px | −0.3px | 28px/33px | заголовки секций |
| `font-h3` | Onest 600 | 24px/30px | — | — | заголовки карточек |
| `font-h4` | Onest 600 | 20px/26px | — | — | подзаголовки, аккордеон |
| `font-lead` | Golos Text 400 | 19px/30px | — | — | подзаголовки hero |
| `font-body-article` | Golos Text 400 | 17px/29px | — | — | текст статьи (колонка 720px) |
| `font-body` | Golos Text 400 | 16px/26px | — | — | основной текст |
| `font-body-s` | Golos Text 400 | 15px/24px | — | — | текст карточек |
| `font-caption` | Golos Text 400 | 13px/19px | — | — | подписи полей, хинты |
| `font-mono-caption` | JetBrains Mono 400 | 13px/20px | — | — | метаданные, аннотации |
| `font-overline` | JetBrains Mono 500 | 12px/16px | +1.6px, CAPS | — | надзаголовки секций |
| `font-button` | Golos Text 600 | 17px/24px (компакт 15px/20px) | — | — | кнопки |

Мобильные переопределения заданы только для `display`, `h1`, `h2` — остальные роли не адаптируются по размеру.

### 4.2. Сетка и размеры

| Токен | Значение | Комментарий |
|---|---|---|
| `space-scale` | 4·8·12·16·20·24·32·40·48·64·88·104 px | все отступы — только из шкалы |
| `radius-scale` | 6·8·10·12·16·999 px | заметки·мини·поля·кнопки·карточки·бейджи |
| `container-content` | 1240px | ширина контента |
| `container-max` | 1352px | контент + поля по 56px |
| `container-article` | 720px | колонка текста статьи |
| `grid-columns` / `grid-gutter` | 12 / 24px | колоночная сетка |
| `page-margin` | 56px desktop / 20px mobile | поля страницы |
| `breakpoints` | 390 · 768 · 920 · 1200 · 1440 px | 920px — порог бургер-меню |
| `header-height` | 72px | липкая шапка |
| `control-height` | 54px (поля/кнопки) · 42px (кнопки компакт) | высоты контролов |
| `hit-target-min` | 44px | минимальная область нажатия (доступность) |
| `border-width` | 2px | все бордеры и рамки |
| `focus-ring` | 2px, offset 2px, rgb(38, 84, 210) | видимый фокус с клавиатуры |
| `motion` | 200ms кнопки · 180ms карточки/ссылки · 300ms аккордеон · 650ms reveal | отключается при `prefers-reduced-motion` |

## 5. Структура проекта

```
src/
  pages/            # роуты (.astro), api/lead.js
  layouts/          # базовый layout, layout статьи
  components/
    ui/             # shadcn (JSX)
    blocks/         # секции страниц: Hero, PainList, ServiceLadder, LeadForm...
  content/
    blog/           # MDX-статьи
    cases/          # MDX-кейсы
  styles/
    tokens.css      # ЕДИНСТВЕННОЕ место определения цветов и размерных переменных
    global.css
```

- Тексты офферов не переписывать «покрасивее» — формулировки заголовков, болей и CTA зафиксированы в плане сайта и меняются только человеком.

## 6. Правила для промптов (напоминание себе)

- Каждый промпт: ссылка на этот файл + конкретная фаза/задача из плана + критерий приёмки.
- Одна задача — один промпт. Не просить «сделай фазы 3–5 разом».
- После генерации проверять: нет `.tsx`, нет hex/hsl в коде, нет rem, нет самовольных зависимостей (`git diff package.json`).
